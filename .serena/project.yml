# list of languages for which language servers are started; choose from:
#   al               bash             clojure          cpp              csharp           csharp_omnisharp
#   dart             elixir           elm              erlang           fortran          go
#   haskell          java             julia            kotlin           lua              markdown
#   nix              perl             php              python           python_jedi      r
#   rego             ruby             ruby_solargraph  rust             scala            swift
#   terraform        typescript       typescript_vts   yaml             zig
# Note:
#   - For C, use cpp
#   - For JavaScript, use typescript
# Special requirements:
#   - csharp: Requires the presence of a .sln file in the project folder.
# When using multiple languages, the first language server that supports a given file will be used for that file.
# The first language is the default language and the respective language server will be used as a fallback.
# Note that when using the JetBrains backend, language servers are not used and this list is correspondingly ignored.
languages:
- java
- typescript  # For Angular frontend

# the encoding used by text files in the project
# For a list of possible encodings, see https://docs.python.org/3.11/library/codecs.html#standard-encodings
encoding: "utf-8"

# whether to use the project's gitignore file to ignore files
# Added on 2025-04-07
ignore_all_files_in_gitignore: true

# list of additional paths to ignore
# same syntax as gitignore, so you can use * and **
# Was previously called `ignored_dirs`, please update your config if you are using that.
# Added (renamed) on 2025-04-07
ignored_paths:
  - "**/build/**"
  - "**/dist/**"
  - "**/node_modules/**"
  - "**/.gradle/**"
  - "**/target/**"
  - "**/.nx/**"
  - "**/coverage/**"
  - "**/.docker-tmp/**"

# whether the project is in read-only mode
# If set to true, all editing tools will be disabled and attempts to use them will result in an error
# Added on 2025-04-18
read_only: false

# list of tool names to exclude. We recommend not excluding any tools, see the readme for more details.
# Below is the complete list of tools for convenience.
# To make sure you have the latest list of tools, and to view their descriptions, 
# execute `uv run scripts/print_tool_overview.py`.
#
#  * `activate_project`: Activates a project by name.
#  * `check_onboarding_performed`: Checks whether project onboarding was already performed.
#  * `create_text_file`: Creates/overwrites a file in the project directory.
#  * `delete_lines`: Deletes a range of lines within a file.
#  * `delete_memory`: Deletes a memory from Serena's project-specific memory store.
#  * `execute_shell_command`: Executes a shell command.
#  * `find_referencing_code_snippets`: Finds code snippets in which the symbol at the given location is referenced.
#  * `find_referencing_symbols`: Finds symbols that reference the symbol at the given location (optionally filtered by type).
#  * `find_symbol`: Performs a global (or local) search for symbols with/containing a given name/substring (optionally filtered by type).
#  * `get_current_config`: Prints the current configuration of the agent, including the active and available projects, tools, contexts, and modes.
#  * `get_symbols_overview`: Gets an overview of the top-level symbols defined in a given file.
#  * `initial_instructions`: Gets the initial instructions for the current project.
#     Should only be used in settings where the system prompt cannot be set,
#     e.g. in clients you have no control over, like Claude Desktop.
#  * `insert_after_symbol`: Inserts content after the end of the definition of a given symbol.
#  * `insert_at_line`: Inserts content at a given line in a file.
#  * `insert_before_symbol`: Inserts content before the beginning of the definition of a given symbol.
#  * `list_dir`: Lists files and directories in the given directory (optionally with recursion).
#  * `list_memories`: Lists memories in Serena's project-specific memory store.
#  * `onboarding`: Performs onboarding (identifying the project structure and essential tasks, e.g. for testing or building).
#  * `prepare_for_new_conversation`: Provides instructions for preparing for a new conversation (in order to continue with the necessary context).
#  * `read_file`: Reads a file within the project directory.
#  * `read_memory`: Reads the memory with the given name from Serena's project-specific memory store.
#  * `remove_project`: Removes a project from the Serena configuration.
#  * `replace_lines`: Replaces a range of lines within a file with new content.
#  * `replace_symbol_body`: Replaces the full definition of a symbol.
#  * `restart_language_server`: Restarts the language server, may be necessary when edits not through Serena happen.
#  * `search_for_pattern`: Performs a search for a pattern in the project.
#  * `summarize_changes`: Provides instructions for summarizing the changes made to the codebase.
#  * `switch_modes`: Activates modes by providing a list of their names
#  * `think_about_collected_information`: Thinking tool for pondering the completeness of collected information.
#  * `think_about_task_adherence`: Thinking tool for determining whether the agent is still on track with the current task.
#  * `think_about_whether_you_are_done`: Thinking tool for determining whether the task is truly completed.
#  * `write_memory`: Writes a named memory (for future reference) to Serena's project-specific memory store.
excluded_tools: []

# initial prompt for the project. It will always be given to the LLM upon activating the project
# (contrary to the memories, which are loaded on demand).
initial_prompt: |
  # HealthData-in-Motion (HDIM) - Enterprise Healthcare Interoperability Platform

  ## Project Overview
  HDIM is an enterprise healthcare platform for HEDIS quality measure evaluation, FHIR R4 compliance, and clinical decision support.

  **Primary Purpose**: Enable healthcare organizations to evaluate clinical quality measures (CQL/HEDIS), identify care gaps, perform risk stratification, and generate quality reports for value-based care contracts.

  **Target Users**: Healthcare payers, ACOs, health systems, and clinical quality teams.

  ## Tech Stack
  - **Backend**: Java 21, Spring Boot 3.x, HAPI FHIR 7.x, Gradle 8.11+ (Kotlin DSL)
  - **Frontend**: Angular 17+, RxJS, Angular Material
  - **Data**: PostgreSQL 15, Redis 7, Apache Kafka 3.x
  - **Platform**: Docker, Kubernetes, Kong API Gateway, HashiCorp Vault

  ## Architecture (28 Microservices)
  **Core Services**:
  - quality-measure-service (8087): HEDIS measure evaluation
  - cql-engine-service (8081): CQL expression evaluation
  - fhir-service (8085): FHIR R4 resource management
  - patient-service (8084): Patient demographics
  - care-gap-service (8086): Care gap detection
  - gateway-service (8001): API gateway & routing

  **Infrastructure**: PostgreSQL (5435), Redis (6380), Kafka (9094), Prometheus (9090), Grafana (3001)

  ## CRITICAL REQUIREMENTS

  ### 1. HIPAA Compliance (MANDATORY)
  - PHI cache TTL MUST be ≤ 5 minutes
  - Cache-Control: no-store, no-cache headers on all PHI endpoints
  - @Audited annotation on all PHI access methods
  - NO PHI in log statements
  - See: `.serena/memories/hipaa-compliance-checklist.md`

  ### 2. Gateway Trust Authentication (GOLD STANDARD)
  - Gateway validates JWT tokens, backend services trust X-Auth-* headers
  - Services MUST use TrustedHeaderAuthFilter + TrustedTenantAccessFilter
  - DO NOT re-validate JWT in backend services
  - DO NOT perform database lookups for user/tenant info
  - See: `.serena/memories/gateway-trust-auth.md`

  ### 3. Multi-Tenant Isolation (ENFORCED)
  - ALL queries MUST filter by tenantId
  - Use @Query with explicit tenant parameter
  - X-Tenant-ID header required on all requests
  - See: `.serena/memories/common-patterns.md`

  ### 4. Entity-Migration Synchronization (PREVENTS PRODUCTION BUGS)
  - JPA entities MUST match Liquibase migrations exactly
  - Use `ddl-auto: validate` in ALL environments (NEVER use update/create)
  - Run EntityMigrationValidationTest before committing
  - NEVER modify existing migrations, create new ones
  - See: `.serena/memories/entity-migration-sync.md`

  ## Build Commands (from backend/ directory)
  ```bash
  ./gradlew build                                    # Build all modules
  ./gradlew :modules:services:SERVICE_NAME:build     # Build specific service
  ./gradlew :modules:services:SERVICE_NAME:bootRun   # Run service
  ./gradlew test                                     # Run all tests
  ```

  ## Docker Commands (from project root)
  ```bash
  docker compose up -d              # Start all services
  docker compose logs -f SERVICE    # View logs
  docker compose ps                 # Check status
  docker compose down               # Stop all
  ```

  ## Serena Memories Available
  Use `read_memory` or `list_memories` to access:
  - architecture-overview.md - System architecture, 28 services, tech stack
  - hipaa-compliance-checklist.md - PHI handling, cache TTL, audit logging
  - gateway-trust-auth.md - Authentication architecture (GOLD STANDARD)
  - entity-migration-sync.md - JPA/Liquibase synchronization rules
  - service-registry.md - Complete service catalog with ports and dependencies
  - common-patterns.md - Controller, service, repository, entity patterns
  - troubleshooting-guide.md - Common issues and solutions

  ## Code Review Checklist (Before Committing)
  - [ ] HIPAA: Cache TTL ≤ 5 minutes for PHI
  - [ ] HIPAA: Cache-Control headers on PHI endpoints
  - [ ] HIPAA: @Audited annotation on PHI access
  - [ ] Multi-tenant: ALL queries filter by tenantId
  - [ ] Auth: @PreAuthorize on all endpoints
  - [ ] Auth: Using TrustedHeaderAuthFilter (NOT JwtAuthenticationFilter)
  - [ ] Database: Entity matches migration exactly
  - [ ] Database: ddl-auto is 'validate' (NOT update/create)
  - [ ] Tests: EntityMigrationValidationTest passes
  - [ ] Logging: No PHI in log statements

  ## Documentation References
  - `CLAUDE.md` - AI coding guidelines (THIS FILE IS LAW)
  - `backend/docs/GATEWAY_TRUST_ARCHITECTURE.md` - Auth architecture
  - `backend/HIPAA-CACHE-COMPLIANCE.md` - PHI cache rules
  - `backend/docs/ENTITY_MIGRATION_GUIDE.md` - Database sync guide
  - `docs/architecture/SYSTEM_ARCHITECTURE.md` - Complete system design

  **When in doubt, consult Serena memories or CLAUDE.md.**

project_name: "hdim-master"
included_optional_tools: []
