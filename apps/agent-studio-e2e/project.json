{
  "name": "agent-studio-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/agent-studio-e2e/src",
  "implicitDependencies": ["clinical-portal"],
  "// targets": "to see all targets run: nx show project agent-studio-e2e --web",
  "targets": {
    "e2e": {
      "executor": "@nx/playwright:playwright",
      "options": {
        "config": "apps/agent-studio-e2e/playwright.config.ts"
      }
    },
    "coverage": {
      "executor": "@nx/playwright:playwright",
      "options": {
        "config": "apps/agent-studio-e2e/playwright.config.ts"
      }
    }
  }
}
