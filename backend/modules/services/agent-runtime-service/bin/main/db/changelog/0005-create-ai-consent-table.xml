<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.29.xsd">

    <changeSet id="0005-create-ai-consent-table" author="hdim">
        <comment>Create ai_consent table for tracking patient consent for AI features</comment>

        <createTable tableName="ai_consent">
            <column name="id" type="UUID">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="patient_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="tenant_id" type="VARCHAR(64)">
                <constraints nullable="false"/>
            </column>
            <column name="feature_type" type="VARCHAR(64)">
                <constraints nullable="false"/>
            </column>
            <column name="consent_level" type="VARCHAR(32)">
                <constraints nullable="false"/>
            </column>
            <column name="llm_data_sharing_consent" type="BOOLEAN" defaultValueBoolean="false"/>
            <column name="cross_border_consent" type="BOOLEAN" defaultValueBoolean="false"/>
            <column name="jurisdictions_consented" type="TEXT[]"/>
            <column name="consented_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="CURRENT_TIMESTAMP"/>
            <column name="expires_at" type="TIMESTAMP WITH TIME ZONE"/>
            <column name="revoked_at" type="TIMESTAMP WITH TIME ZONE"/>
            <column name="consent_source" type="VARCHAR(64)"/>
            <column name="consent_document_id" type="VARCHAR(64)"/>
            <column name="created_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="CURRENT_TIMESTAMP"/>
            <column name="updated_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="CURRENT_TIMESTAMP"/>
        </createTable>

        <sql>
            ALTER TABLE ai_consent
                ADD CONSTRAINT chk_consent_level
                CHECK (consent_level IN ('EXPLICIT_OPT_IN', 'OPT_OUT_AVAILABLE', 'OPT_OUT'));
        </sql>

        <sql>
            ALTER TABLE ai_consent
                ADD CONSTRAINT chk_feature_type
                CHECK (feature_type IN ('AI_CLINICAL_SUMMARY', 'AI_RISK_PREDICTION', 'AI_CARE_RECOMMENDATIONS', 'AI_DOCUMENTATION', 'AI_OUTREACH'));
        </sql>

        <createIndex tableName="ai_consent" indexName="idx_ai_consent_patient">
            <column name="patient_id"/>
            <column name="tenant_id"/>
        </createIndex>

        <sql>
            CREATE INDEX IF NOT EXISTS idx_ai_consent_feature
                ON ai_consent(tenant_id, feature_type) WHERE revoked_at IS NULL;
        </sql>

        <rollback>
            <dropTable tableName="ai_consent"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
