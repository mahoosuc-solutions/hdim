{
  "name": "data-enrichment-service",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "backend/modules/services/data-enrichment-service/src",
  "tags": [
    "backend",
    "service"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/backend/modules/services/data-enrichment-service/build"
      ],
      "options": {
        "command": "./backend/gradlew -p backend :modules:services:data-enrichment-service:build"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./backend/gradlew -p backend :modules:services:data-enrichment-service:test"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./backend/gradlew -p backend :modules:services:data-enrichment-service:clean"
      }
    },
    "coverage": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./backend/gradlew -p backend :modules:services:data-enrichment-service:test :modules:services:data-enrichment-service:jacocoTestReport :modules:services:data-enrichment-service:jacocoTestCoverageVerification"
      }
    }
  }
}
