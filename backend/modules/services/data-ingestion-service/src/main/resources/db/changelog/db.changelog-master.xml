<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <!-- Data Ingestion Service Changelog
         
         Purpose: Minimal schema for tracking ingestion sessions (optional).
         Most data flows through FHIR/Care Gap/Quality Measure services.
         
         This service is stateless by design - session tracking is in-memory.
         Database is primarily for potential future audit trail of ingestion operations.
    -->

    <!-- Future: Add changesets if persistent session tracking is needed -->
    <!-- For now, service uses in-memory session storage (ConcurrentHashMap) -->

</databaseChangeLog>
