{
  "name": "ehr-connector-service",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "backend/modules/services/ehr-connector-service/src",
  "tags": [
    "backend",
    "service"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/backend/modules/services/ehr-connector-service/build"
      ],
      "options": {
        "command": "./backend/gradlew -p backend :modules:services:ehr-connector-service:build"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./backend/gradlew -p backend :modules:services:ehr-connector-service:test"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./backend/gradlew -p backend :modules:services:ehr-connector-service:clean"
      }
    },
    "coverage": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./backend/gradlew -p backend :modules:services:ehr-connector-service:test :modules:services:ehr-connector-service:jacocoTestReport :modules:services:ehr-connector-service:jacocoTestCoverageVerification"
      }
    }
  }
}
