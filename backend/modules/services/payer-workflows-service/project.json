{
  "name": "payer-workflows-service",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "backend/modules/services/payer-workflows-service/src",
  "tags": [
    "backend",
    "service"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/backend/modules/services/payer-workflows-service/build"
      ],
      "options": {
        "command": "./backend/gradlew -p backend :modules:services:payer-workflows-service:build"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./backend/gradlew -p backend :modules:services:payer-workflows-service:test"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./backend/gradlew -p backend :modules:services:payer-workflows-service:clean"
      }
    },
    "coverage": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./backend/gradlew -p backend :modules:services:payer-workflows-service:test :modules:services:payer-workflows-service:jacocoTestReport :modules:services:payer-workflows-service:jacocoTestCoverageVerification"
      }
    }
  }
}
