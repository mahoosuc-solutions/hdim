{
  "name": "prior-auth-service",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "backend/modules/services/prior-auth-service/src",
  "tags": [
    "backend",
    "service"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/backend/modules/services/prior-auth-service/build"
      ],
      "options": {
        "command": "./backend/gradlew -p backend :modules:services:prior-auth-service:build"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./backend/gradlew -p backend :modules:services:prior-auth-service:test"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./backend/gradlew -p backend :modules:services:prior-auth-service:clean"
      }
    },
    "coverage": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./backend/gradlew -p backend :modules:services:prior-auth-service:test :modules:services:prior-auth-service:jacocoTestReport :modules:services:prior-auth-service:jacocoTestCoverageVerification"
      }
    }
  }
}
