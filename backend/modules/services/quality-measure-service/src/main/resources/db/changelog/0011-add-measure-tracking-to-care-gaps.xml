<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.23.xsd">

    <changeSet id="0008-add-measure-tracking-to-care-gaps" author="healthdata-team">
        <comment>Add measure result tracking and auto-creation flags to care gaps table</comment>

        <!-- Add measure_result_id column to link care gaps to measure results -->
        <addColumn tableName="care_gaps">
            <column name="measure_result_id" type="uuid">
                <constraints nullable="true"/>
            </column>
        </addColumn>

        <!-- Add created_from_measure flag to identify auto-created gaps -->
        <addColumn tableName="care_gaps">
            <column name="created_from_measure" type="boolean" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>
        </addColumn>

        <!-- Add foreign key constraint to quality_measure_results -->
        <addForeignKeyConstraint
            constraintName="fk_care_gap_measure_result"
            baseTableName="care_gaps"
            baseColumnNames="measure_result_id"
            referencedTableName="quality_measure_results"
            referencedColumnNames="id"
            onDelete="SET NULL"
            onUpdate="CASCADE"/>

        <!-- Create composite index for efficient measure-based gap lookups -->
        <createIndex indexName="idx_cg_patient_measure_status" tableName="care_gaps">
            <column name="patient_id"/>
            <column name="measure_result_id"/>
            <column name="status"/>
        </createIndex>

        <!-- Create index on measure_result_id for reverse lookups -->
        <createIndex indexName="idx_cg_measure_result" tableName="care_gaps">
            <column name="measure_result_id"/>
        </createIndex>

        <!-- Create index for auto-created gaps queries -->
        <createIndex indexName="idx_cg_created_from_measure" tableName="care_gaps">
            <column name="created_from_measure"/>
            <column name="tenant_id"/>
        </createIndex>

        <rollback>
            <dropIndex indexName="idx_cg_created_from_measure" tableName="care_gaps"/>
            <dropIndex indexName="idx_cg_measure_result" tableName="care_gaps"/>
            <dropIndex indexName="idx_cg_patient_measure_status" tableName="care_gaps"/>
            <dropForeignKeyConstraint baseTableName="care_gaps" constraintName="fk_care_gap_measure_result"/>
            <dropColumn tableName="care_gaps" columnName="created_from_measure"/>
            <dropColumn tableName="care_gaps" columnName="measure_result_id"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
