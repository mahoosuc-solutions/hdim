package com.healthdata.sales.entity;

/**
 * Target type for email sequences
 */
public enum TargetType {
    LEAD,              // Target leads
    CONTACT,           // Target contacts
    BOTH               // Can target either leads or contacts
}
