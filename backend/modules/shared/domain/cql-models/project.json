{
  "name": "modules-shared-domain-cql-models",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "backend/modules/shared/domain/cql-models/src",
  "tags": [
    "backend",
    "lib"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/backend/modules/shared/domain/cql-models/build"
      ],
      "options": {
        "command": "./backend/gradlew -p backend :modules:shared:domain:cql-models:build"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./backend/gradlew -p backend :modules:shared:domain:cql-models:test"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./backend/gradlew -p backend :modules:shared:domain:cql-models:clean"
      }
    },
    "coverage": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./backend/gradlew -p backend :modules:shared:domain:cql-models:test :modules:shared:domain:cql-models:jacocoTestReport :modules:shared:domain:cql-models:jacocoTestCoverageVerification"
      }
    }
  }
}
