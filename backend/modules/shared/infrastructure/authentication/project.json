{
  "name": "modules-shared-infrastructure-authentication",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "backend/modules/shared/infrastructure/authentication/src",
  "tags": [
    "backend",
    "lib"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/backend/modules/shared/infrastructure/authentication/build"
      ],
      "options": {
        "command": "./backend/gradlew -p backend :modules:shared:infrastructure:authentication:build"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./backend/gradlew -p backend :modules:shared:infrastructure:authentication:test"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./backend/gradlew -p backend :modules:shared:infrastructure:authentication:clean"
      }
    },
    "coverage": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./backend/gradlew -p backend :modules:shared:infrastructure:authentication:test :modules:shared:infrastructure:authentication:jacocoTestReport :modules:shared:infrastructure:authentication:jacocoTestCoverageVerification"
      }
    }
  }
}
