<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="01-create-refresh-tokens-table" author="system">
        <comment>Create refresh_tokens table for JWT authentication</comment>

        <createTable tableName="refresh_tokens">
            <column name="id" type="UUID">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="token" type="VARCHAR(1000)">
                <constraints nullable="false" unique="true"/>
            </column>

            <column name="user_id" type="UUID">
                <constraints nullable="false"
                             foreignKeyName="fk_refresh_tokens_user_id"
                             references="users(id)"
                             deleteCascade="true"/>
            </column>

            <column name="expires_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="revoked_at" type="TIMESTAMP">
                <constraints nullable="true"/>
            </column>

            <column name="ip_address" type="VARCHAR(45)">
                <constraints nullable="true"/>
            </column>

            <column name="user_agent" type="VARCHAR(500)">
                <constraints nullable="true"/>
            </column>
        </createTable>

        <!-- Index on token for fast lookup -->
        <createIndex indexName="idx_refresh_tokens_token" tableName="refresh_tokens" unique="true">
            <column name="token"/>
        </createIndex>

        <!-- Index on user_id for querying user's tokens -->
        <createIndex indexName="idx_refresh_tokens_user_id" tableName="refresh_tokens">
            <column name="user_id"/>
        </createIndex>

        <!-- Index on expires_at for cleanup queries -->
        <createIndex indexName="idx_refresh_tokens_expires_at" tableName="refresh_tokens">
            <column name="expires_at"/>
        </createIndex>

        <!-- Composite index for checking valid tokens -->
        <createIndex indexName="idx_refresh_tokens_token_revoked" tableName="refresh_tokens">
            <column name="token"/>
            <column name="revoked_at"/>
        </createIndex>

        <rollback>
            <dropTable tableName="refresh_tokens"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
