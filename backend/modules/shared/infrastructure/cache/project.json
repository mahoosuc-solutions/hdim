{
  "name": "modules-shared-infrastructure-cache",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "backend/modules/shared/infrastructure/cache/src",
  "tags": [
    "backend",
    "lib"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/backend/modules/shared/infrastructure/cache/build"
      ],
      "options": {
        "command": "./backend/gradlew -p backend :modules:shared:infrastructure:cache:build"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./backend/gradlew -p backend :modules:shared:infrastructure:cache:test"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./backend/gradlew -p backend :modules:shared:infrastructure:cache:clean"
      }
    },
    "coverage": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./backend/gradlew -p backend :modules:shared:infrastructure:cache:test :modules:shared:infrastructure:cache:jacocoTestReport :modules:shared:infrastructure:cache:jacocoTestCoverageVerification"
      }
    }
  }
}
