{
  "name": "modules-shared-infrastructure-security",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "backend/modules/shared/infrastructure/security/src",
  "tags": [
    "backend",
    "lib"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/backend/modules/shared/infrastructure/security/build"
      ],
      "options": {
        "command": "./backend/gradlew -p backend :modules:shared:infrastructure:security:build"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./backend/gradlew -p backend :modules:shared:infrastructure:security:test"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./backend/gradlew -p backend :modules:shared:infrastructure:security:clean"
      }
    },
    "coverage": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./backend/gradlew -p backend :modules:shared:infrastructure:security:test :modules:shared:infrastructure:security:jacocoTestReport :modules:shared:infrastructure:security:jacocoTestCoverageVerification"
      }
    }
  }
}
