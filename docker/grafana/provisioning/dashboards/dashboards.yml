apiVersion: 1

providers:
  - name: 'HDIM Dashboards'
    orgId: 1
    folder: 'HDIM'
    folderUid: 'hdim-folder'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
