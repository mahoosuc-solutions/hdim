apiVersion: apps/v1
kind: Deployment
metadata:
  name: quality-measure-service
  labels:
    app: quality-measure-service
    app.kubernetes.io/part-of: hdim-platform
    tier: core
spec:
  replicas: 1
  selector:
    matchLabels:
      app: quality-measure-service
  template:
    metadata:
      labels:
        app: quality-measure-service
        tier: core
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values: ["quality-measure-service"]
                topologyKey: kubernetes.io/hostname
      containers:
        - name: quality-measure-service
          image: ghcr.io/mahoosuc-solutions/hdim/quality-measure-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8087
          envFrom:
            - configMapRef:
                name: hdim-common-config
            - configMapRef:
                name: hdim-service-urls
            - secretRef:
                name: hdim-db-credentials
            - secretRef:
                name: hdim-redis-credentials
            - secretRef:
                name: hdim-kafka-credentials
            - secretRef:
                name: hdim-jwt-credentials
          env:
            - name: OTEL_SERVICE_NAME
              value: quality-measure-service
            - name: HEALTH_TENANT_ID
              value: service-health-quality-measure
            - name: SPRING_DATASOURCE_URL
              value: jdbc:postgresql://postgres:5432/quality_measure_db
            - name: JAVA_TOOL_OPTIONS
              value: "-Djava.net.preferIPv4Stack=true"
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /quality-measure/actuator/health/liveness
              port: 8087
            initialDelaySeconds: 120
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /quality-measure/actuator/health/readiness
              port: 8087
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: quality-measure-service
  labels:
    app: quality-measure-service
spec:
  type: ClusterIP
  ports:
    - port: 8087
      targetPort: 8087
  selector:
    app: quality-measure-service
