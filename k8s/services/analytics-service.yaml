---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
  namespace: hdim
  labels:
    app: analytics-service
    app.kubernetes.io/part-of: hdim-platform
    tier: analytics
spec:
  replicas: 1
  selector:
    matchLabels:
      app: analytics-service
  template:
    metadata:
      labels:
        app: analytics-service
        tier: analytics
    spec:
      containers:
        - name: analytics-service
          image: hdim/analytics-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8092
          envFrom:
            - configMapRef:
                name: hdim-common-config
            - configMapRef:
                name: hdim-service-urls
            - secretRef:
                name: hdim-db-credentials
            - secretRef:
                name: hdim-redis-credentials
            - secretRef:
                name: hdim-kafka-credentials
            - secretRef:
                name: hdim-jwt-credentials
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /analytics/actuator/health/liveness
              port: 8092
            initialDelaySeconds: 90
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /analytics/actuator/health/readiness
              port: 8092
            initialDelaySeconds: 45
            periodSeconds: 10
            timeoutSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: hdim
  labels:
    app: analytics-service
spec:
  type: ClusterIP
  ports:
    - port: 8092
      targetPort: 8092
  selector:
    app: analytics-service
