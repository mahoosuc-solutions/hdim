---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: quality-measure-service
  namespace: hdim
  labels:
    app: quality-measure-service
    app.kubernetes.io/part-of: hdim-platform
    tier: core
spec:
  replicas: 1
  selector:
    matchLabels:
      app: quality-measure-service
  template:
    metadata:
      labels:
        app: quality-measure-service
        tier: core
    spec:
      containers:
        - name: quality-measure-service
          image: hdim/quality-measure-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8087
          envFrom:
            - configMapRef:
                name: hdim-common-config
            - configMapRef:
                name: hdim-service-urls
            - secretRef:
                name: hdim-db-credentials
            - secretRef:
                name: hdim-redis-credentials
            - secretRef:
                name: hdim-kafka-credentials
            - secretRef:
                name: hdim-jwt-credentials
          resources:
            requests:
              cpu: "200m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /quality-measure/actuator/health/liveness
              port: 8087
            initialDelaySeconds: 90
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /quality-measure/actuator/health/readiness
              port: 8087
            initialDelaySeconds: 45
            periodSeconds: 10
            timeoutSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: quality-measure-service
  namespace: hdim
  labels:
    app: quality-measure-service
spec:
  type: ClusterIP
  ports:
    - port: 8087
      targetPort: 8087
  selector:
    app: quality-measure-service
